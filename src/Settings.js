/* eslint-disable */

import React from 'react';
import './Settings.css';


const Settings = (props) => (
    <div className="Settings-container">
      This is where the User App Settings live!
    </div>
)

export default Settings;